# AutoSOC production environment template
# Copy this to .env for local development or deployment configuration.

NODE_ENV=production
PORT=5000

# Optional: control CORS access for browser clients.
# Use comma-separated origins when needed, or leave empty for permissive local/demo behavior.
CORS_ORIGIN=*

# Optional logging level.
LOG_LEVEL=info
